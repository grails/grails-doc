h1. hasErrors

h2. Purpose

Check if a controller has errors associated with it.

h2. Examples

{code:java}
class DemoController {

    def generateReport(int size) {
        if(hasErrors()) {
            errors.allErrors.each {
                println it
            }
        }
    }
}
{code}

See [errors|controllers].
