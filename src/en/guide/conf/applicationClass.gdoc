Every new Grails application features an @Application@ class witin the the @grails-app/init@ directory.

The @Application@ class subclasses the [GrailsAutoConfiguration|api:grails.boot.config.GrailsAutoConfiguration] class and features a @static void main@ method, meaning it can be run as a regular application.
