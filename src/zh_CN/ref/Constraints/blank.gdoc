h1. blank

h2. Purpose

Validates that a String value is not blank.

h2. Examples

{code:java}
login blank: false
{code}

h2. Description

Set to @false@ if a string value cannot be blank.

Error Code: @className.propertyName.blank@

{note}
If the string is @null@, it won't validate with @blank: true@. In this case, set the [nullable|constraints] constraint to @true@.
{note}
