h1. blank 

h2. Purpose

To validate that a String value is not blank.

h2. Examples

{code:java}
login(blank:false)
{code}

h2. Description

Set to @false@ if a string value cannot be blank.

Error Code: @className.propertyName.blank@

{note}
Setting @blank=true@ won't validate if the String is @null@. In this case, use [nullable|constraints] constraint set @true@.
{note}

